import WorldObject from "../../WorldObject";
import config from "./config";
import { Utils } from "../../AppUtils";

export default class SceneBg extends WorldObject {
  constructor(objRenderer) {
    super(objRenderer);

    // create background as sky color
  }
}
