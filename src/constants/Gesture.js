// Gesture type definition
const GestureType = {
  Swipe: "swipe",
  Press: "press",
  // Tap: "tap",
  DoubleTap: "doubletap"
};

const GestureTypeList = [
  GestureType.Press,
  GestureType.Swipe,
  // GestureType.Tap,
  GestureType.DoubleTap
];

export { GestureType, GestureTypeList };
